import React from 'react';
import { api } from './api';

const App = () => {
    console.log(api);

    return (
        <div>
            <div>
                <div></div>
            </div>
        </div>
    );
};

export default App;
